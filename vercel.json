{
    "env": {
        "PYTHONPATH": "src/"
    },
    "rewrites": [{
        "source": "/(.*)",
        "destination": "api/main.py"
    }],
    "functions": {
        "api/main.py": {
            "maxDuration": 30
        }
    },
    "buildCommand": "pip3 install -r requirements.txt;"
}